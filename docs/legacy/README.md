# Legacy Notes

This directory contains historical notes, mockups, and bridge-era references. These files are not the supported install path.

Use the root `README.md` and `integrations/codex/README.md` for the current hook-only Nomi flow.
